---
"knots": patch
---

Detect managed skill drift in `kno doctor` by comparing deployed managed
`SKILL.md` files against rendered content. `kno doctor --fix` now reconciles
both missing and drifted managed skills.
