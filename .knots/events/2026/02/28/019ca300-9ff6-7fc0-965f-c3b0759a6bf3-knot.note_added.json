{
  "event_id": "019ca300-9ff6-7fc0-965f-c3b0759a6bf3",
  "occurred_at": "2026-02-28T06:47:33.110678Z",
  "knot_id": "knots-cb7f",
  "type": "knot.note_added",
  "data": {
    "agentname": "unknown",
    "content": "## Implementation Plan: Git Hooks for Auto-Sync\n\n### Overview\nAdd a `kno hooks` subcommand that generates/manages git hooks calling `kno sync` on post-commit and post-merge. Integrate into `kno init`, `kno upgrade`, and `kno doctor`.\n\n### Design Decisions\n- **Hook types**: `post-commit` (sync after local commits) and `post-merge` (sync after pulls/merges). Both are non-blocking — they run `kno sync` in the background so they don't slow down git operations.\n- **Marker pattern**: Use `knots-managed-<hook-name>` marker (consistent with existing `knots-managed-pre-push-hook` in scripts/repo/).\n- **Existing hook preservation**: If an unmanaged hook already exists, move it to `<hook>.local` and chain-call it from the managed hook (same pattern as install-hooks.sh).\n- **core.hooksPath support**: Respect `git config core.hooksPath` (reuse pattern from `detect_beads_hooks()` in remote_init.rs).\n- **Idempotent**: Running install twice is safe — detects marker and skips if present. Upgrade regenerates the hook content but preserves `.local` chain.\n\n### Hook Template\n```bash\n#!/usr/bin/env bash\n# knots-managed-post-commit-hook\nif [ -x \"$(dirname \"$0\")/post-commit.local\" ]; then\n  \"$(dirname \"$0\")/post-commit.local\" \"$@\"\nfi\nkno sync >/dev/null 2>&1 &\n```\n\n### New Module: `src/git_hooks.rs` (~120 lines)\n- `const MANAGED_HOOKS: &[&str] = &[\"post-commit\", \"post-merge\"]`\n- `const KNOTS_HOOK_MARKER: &str = \"knots-managed\"`\n- `fn resolve_hooks_dir(repo_root) -> PathBuf` — reads `core.hooksPath`, falls back to `.git/hooks`\n- `fn is_knots_managed(path) -> bool` — reads file, checks for marker\n- `fn install_hook(hooks_dir, hook_name) -> Result<HookInstallOutcome>` — generates hook, preserves existing to `.local`\n- `fn uninstall_hook(hooks_dir, hook_name) -> Result<bool>` — removes managed hook, restores `.local` if present\n- `fn check_hooks(repo_root) -> DoctorCheck` — returns Pass/Warn based on managed hook presence\n- `pub fn install_hooks(repo_root) -> Result<HooksSummary>`\n- `pub fn uninstall_hooks(repo_root) -> Result<HooksSummary>`\n- `pub fn hooks_status(repo_root) -> HooksStatusReport`\n\n### CLI Changes\n1. **`src/cli.rs`**: Add `Commands::Hooks(HooksArgs)` variant\n2. **`src/cli_ops.rs`**: Add `HooksArgs { command: HooksSubcommands }` and `HooksSubcommands { Install, Uninstall, Status }`\n3. **`src/main.rs`**: Dispatch `Commands::Hooks` before `app` init (hooks don't need DB access)\n\n### Integration Points\n1. **`src/init.rs` → `init_all()`**: After remote branch init, call `git_hooks::install_hooks()` with progress output\n2. **`src/doctor.rs` → `run_doctor()`**: Add `git_hooks::check_hooks()` to the checks vec\n3. **`install.sh`**: No change needed — hooks call `kno` by name (in PATH), so binary upgrades automatically take effect\n4. **`src/self_manage.rs`**: After `kno upgrade` completes, print a hint: \"run `kno hooks install` to update hook templates\"\n\n### Test Strategy\n- Unit tests in `src/git_hooks.rs`: install/uninstall/idempotency/existing-hook-preservation/core.hooksPath\n- Doctor check test in `src/doctor_tests_ext.rs`: verify hook check produces Warn when missing, Pass when installed\n- Init integration test: verify `init_all` installs hooks\n- All tests use temp git repos (same pattern as init.rs and remote_init.rs tests)\n\n### Files to Create/Modify\n| File | Action |\n|---|---|\n| `src/git_hooks.rs` | Create — hook install/uninstall/detect/doctor-check logic |\n| `src/cli.rs` | Modify — add `Commands::Hooks(HooksArgs)` |\n| `src/cli_ops.rs` | Modify — add `HooksArgs`, `HooksSubcommands` |\n| `src/main.rs` | Modify — add `mod git_hooks`, dispatch `Hooks` command |\n| `src/doctor.rs` | Modify — add `check_hooks()` call in `run_doctor()` |\n| `src/init.rs` | Modify — call `install_hooks()` in `init_all()` |\n| `src/doctor_tests_ext.rs` | Modify — add hook check test |\n\n### Complexity & Risk\n- **Complexity**: Medium — mostly new code in a self-contained module, small touchpoints in existing modules\n- **Risk**: Low — hooks are non-blocking (`&` background), existing hook preservation prevents data loss, marker-based detection is robust",
    "datetime": "2026-02-28T06:47:33.110678Z",
    "entry_id": "019ca300-9ff6-7fc0-965f-c3a1cc1d41a5",
    "model": "unknown",
    "username": "unknown",
    "version": "unknown"
  }
}
